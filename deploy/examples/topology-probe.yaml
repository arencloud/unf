apiVersion: v1
kind: Service
metadata:
  name: topology-probe
  namespace: frontend
spec:
  selector:
    app.kubernetes.io/name: client
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
